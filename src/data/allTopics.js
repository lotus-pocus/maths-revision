import statistics from "./statistics";
import geometry from "./geometry";

// All topics combined — add new subject arrays here as they are built
const allTopics = [
  ...statistics,
  ...geometry,
];

export default allTopics;